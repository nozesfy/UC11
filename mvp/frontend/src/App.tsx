// Importa o componente Router que contém as definições de rotas
import Router from './Router'

// Componente principal da aplicação — apenas renderiza o Router
function App() {
  return <Router />
}

// Exporta o App como default para ser usado no main.tsx
export default App
